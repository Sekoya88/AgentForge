"""
Modal training stub — replace body with real Unsloth/TRL training.

Deploy: `modal deploy modal_functions/train.py` (requires `modal` CLI + account).
"""

# import modal
#
# app = modal.App("agentforge-train")
#
#
# @app.function(timeout=3600, gpu="A10G")
# def train(config: dict) -> dict:
#     """Expected keys: base_model, dataset_path, hyperparams (see finetune_jobs)."""
#     return {"status": "completed", "metrics": {}, "model_output_path": ""}
