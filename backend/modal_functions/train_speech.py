"""DEPRECATED: speech training lives in ``train.py``.

``train_speech_model`` is registered on app **agentforge-finetune** next to ``train_model``.

Deploy once (includes LLM SFT + speech stubs)::

    modal deploy backend/modal_functions/train.py

Do not deploy this file; it no longer defines a Modal app.
"""
